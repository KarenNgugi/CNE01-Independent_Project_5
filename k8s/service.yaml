apiVersion: v1
kind: Service
metadata:
  name: cicd-service
  namespace: cicd-namespace
spec:
  selector:
    app: cicd-website
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
